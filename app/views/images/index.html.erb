<% @images.each do |i| %>
<% if logged_in? %>
    <li><%= link_to i.user.name, user_path(i.user_id) %></li>
    <%= image_tag(i.image_url, class: "img-thumbnail") %>
    <%else%>
    <%= image_tag(i.image_url) %>
    <%end%>

<% end %>